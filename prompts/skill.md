你是一个专业的文档优化助手，需要根据以下要求对 `video-creator` skill 中的 `SKILL.md` 进行重构。

**任务目标**  
在不改变 `video-creator` 现有目录结构的前提下，将 `SKILL.md` 的内容合理拆分、迁移到对应目录下的文件中，并进行同类内容的合并与优化，使整体功能得到增强，而非减弱或丢失。

**具体约束与要求**  
1. **严格保持目录结构**  
   - 不得新增、删除或重命名任何目录和文件（包括 `SKILL.md` 本身的路径和文件名）。  
   - 仅可修改现有文件的内容，或将 `SKILL.md` 中的内容移动到已有的合适文件中。  

2. **内容提取与迁移**  
   - 分析 `SKILL.md` 中的各部分内容，将其归类到最相关的已有文件中（如 `README.md`、各子目录下的说明文件、示例文件、脚本注释等）。  
   - 每段内容只能迁移一次，避免重复。迁移后原 `SKILL.md` 中应只保留必要的索引或引用，或完全清空（根据实际情况优化，但文件本身必须保留）。  

3. **同类合并与优化**  
   - 对于目标文件中已有的相似或重复内容，进行去重、合并和精炼。  
   - 优化语言表达和结构，提升可读性和可维护性，确保逻辑清晰、信息密度更高。  
   - 合并后的内容必须覆盖 `SKILL.md` 原有关键功能说明、使用方式、参数说明、示例等，不得遗漏任何功能点。  

4. **功能增强**  
   - 在保持原有信息不丢失的基础上，可以适当补充缺失的上下文、参数默认值、异常处理说明等，让文档更完善。  
   - 检查所有交叉引用、相对路径、命令示例等，确保它们在迁移后依然有效，必要时进行适配性修正。  
   - 考虑从 `SKILL.md` 中提取的内容如何与现有文件协同，形成更完整的知识体系，而不是简单剪切粘贴。  

**执行步骤建议**  
1. 扫描 `video-creator` 完整目录结构，记录所有文件及其作用。  
2. 逐段解析 `SKILL.md`，为每段内容标注最合适的归属文件。  
3. 对每个归属文件：  
   - 读取当前内容；  
   - 将新提取的同类内容与现有内容进行合并和优化；  
   - 更新文件，保证格式统一、信息完整。  
4. 整理 `SKILL.md` 剩余部分，可改为指向其他文件的导航索引，或直接清空（确保核心功能说明不丢失）。  
5. 全局检查，确认无功能说明遗漏、无死链、无矛盾。  

**输出要求**  
- 给出修改后的完整文件内容（仅针对被修改的文件），并对每个文件的改动进行简要说明。  
- 如果 `SKILL.md` 被清空或改为索引，说明其最终内容和定位。  
- 提供一份变更总结，说明如何实现了功能增强。  

请基于以上指令，开始处理 `video-creator` 下的 `SKILL.md` 优化任务。